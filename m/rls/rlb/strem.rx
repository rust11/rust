/* file -  st_rem - remove leading substring */
#include "f:\m\rid\rider.h"
#include "f:\m\rid\stdef.h"
char *st_rem(
char *mod ,
register char *tar )
{ register char *pst ;
  if( (pst = st_scn (mod, tar)) == 0)return ( 0 );
  st_mov (pst, tar);
  return ( tar);
} 
