SMS: - RUST System Macro libraries

The two RUST macro libraries contained here are:

RUST.MLB  RUST/RT-11 macros and definitions (see RUST.COM)
SM.MLB	  PDP-11 Structured Macro system (see SM.COM)

SMS: contains VAX/VMS definitions used in an earlier
implementation.
