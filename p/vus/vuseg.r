
;	RUST creates directories with two free segments
;	squeeze
;	squeeze directory first
;	array of entries per segments
;	combine all possible adjacent segments
;	unlink freed-up segments
;	use segment hopping to complete directory squeeze
;
;	copy files down
;	jigsaw around .BAD files

