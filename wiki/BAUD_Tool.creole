==BAUD==

BAUD.SAV displays the baud rate of a serial line. 

You specify the output CSR address of the line to be tested.
[[code format="text"]]
  .baud /c:177564
  ?BAUD-Clock: 50 hertz, Terminal: 9600 baud.
[[code]]
* Requires inactive unmapped system with a system clock.
* Typing anything aborts the test.
* Specifying the input CSR address is a common error.

