==EI:==

The Rust EIS emulator driver EI:

* EIS is the PDP-11 Extended Instruction Set
* EIS supplies integer mul, div, ash etc instructions
* The RUST/SJ startup command file loads EI: on machines lacking EIS
* All RUST applications may assume EIS is available
