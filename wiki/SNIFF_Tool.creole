==SNIFF==

SNIFF.SAV is a simple Ethernet network traffic sniffer.
[[code format="text"]]
  .r sniff 
[[code]]
* SNIFF decodes a TCP/IP subset and some SMB messages.
* Since SNIFF requires NQ: it runs only on Q-bus systems running RUST/XM or RT-11/XM.
* Another option is to use the Baldwin SJ NQ: driver.
