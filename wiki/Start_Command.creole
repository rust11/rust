==START [address]==

Starts execution of a program loaded in memory. 

**Address**
The address at which to start the program. 
The default address is the start address specified by the program.

[[code format="text"]]
  .get vip           ! load VIP.SAV
  .start 1000        ! start VIP at location 1000
[[code]]
* The START command must be entered as "STA".
* Usually follows the GET command.

